import { defineConfig } from 'vite';

export default defineConfig({
  base: '/<tu-repositorio>/', // Cambia esto a tu nombre de repositorio
  build: {
    outDir: 'dist',
  },
  // Otras configuraciones...
});
